# Prospective no-fit finite-difference assessment

2026-09-19. Prospective policy based on known diagnostic evidence. At the freeze, no new case selection, prediction, reduction of this gate, or trial has been executed. The policy and deterministic input selector are committed together before any such execution. Root and independent source-only reviews found no concrete blocker in the draft. This follows the runtime-only constant-materialization correction; it changes no model, fitting rule, precision option or learned artifact.

## Question and preserved history

The fixed-grid diagnosis attributes the original 11 Cascade observation-history failures primarily to finite-step truncation; smaller steps eventually increase arithmetic error. That is development evidence for this assessment, not fresh confirmation. The original qualification and unchanged h32=2^-7 test remain failed in their original records. Any use of this replacement for adoption is an explicit prospective policy decision, never a retroactive passing result.

Test the computational derivative of the learned smooth mean using independent AD parity, a fixed convergent finite-difference construction, and finite endpoint response at the actual float32 coordinates. These establish numerical usability only. They do not establish that a derivative equals the physical plant's sensitivity, or qualify uncertainty, updates or control.

## Models, regression and confirmation inputs

Use the unchanged saved Crazyflow/Cascade public artifacts and the source-isolated historical full-quadratic oracle at commit `8b61830c9c353dbb25edc6e63a76886d0ce9b9d3`. No fits, initialization, optimization, simulations or new physical truth. Bind the corrected public source/runtime independently from the historical source; require unchanged model bytes and fingerprints. Exact source commits and all external SHA256 values must be fixed in the final request before execution.

Regression roster: all original 33 cases, comprising five nominal artificial horizon cases, four existing representability stress horizon cases, and 24 actual mean cases. The four named stress cases keep their original diagnostic status; every actual case remains mandatory. Save the old h64/h32 flags unchanged alongside the new assessment. Re-execution or reduction of these already inspected cases is regression evidence only.

Confirmation roster: exactly **eight additional factual mean inputs**, four per simulator, selected input-only from the original sealed physical cohort. Per simulator:

1. Read its original `data/queries.json`, authenticated by the existing data seal. Keep factual queries in two strata: primary and shifted, where shifted is the same four declared non-primary scopes.
2. Exclude all original numerical query identities, including response identities represented by the original 24 mean branches. Also exclude an input triple byte-identical to any old actual mean; compare only input bytes. This is fresh numerical input evidence within the same cohort, not new recordings or new simulator data. Existing parents can recur and must be reported.
3. Apply the committed exporter's existing input eligibility only: explicit history eligibility; exact maximum-horizon contract shapes; finite complete `past_states`, `past_inputs`, and `future_inputs`; original float64 layout. Do not read target/validity masks for the future, predictions, residuals, derivative magnitudes, or old diagnostic outcomes to select a query.
4. Sort all eligible identities within the simulator by ordinary `(parent,id)` lexicographic order. Deterministically deduplicate exact three-array input triples across both strata, retaining the first sorted identity for each triple; only then partition the retained identities by stratum and choose first and last distinct query in each stratum. Thus all four selected triples within a simulator are unique, as well as absent from the original actual inputs. If a stratum lacks two, declare insufficient confirmation evidence; no replacement rule or shorter horizon. Selection yields exactly four factual means per simulator, with no response branches added.
5. Save the complete selector/input manifest and its external SHA before any prediction. Bind simulator, parent/segment/source rows, query identity, model, source data seal and exact three input arrays. Replay reconstructs selection and exact bytes. Physical forecasts on this old cohort have already been evaluated historically; only these inputs are untouched by this FD diagnostic. Do not describe them as previously unseen physical evidence.

The combined runner has **41 cases: 37 mandatory nominal/actual and four unchanged diagnostic stress cases**. Keep the original 24 actual regressions and eight actual confirmation inputs separately visible. Both mandatory subsets must pass.

## Exact functions, coefficients, directions and endpoints

Let theta be the original nine NumPy64 parameter arrays and eight NumPy64 norm arrays, without rounding or reinitialization. Let `F64(theta,x)` be the isolated historical quadratic rollout in float64. For each single query (batch example zero for existing artificial batches), define

`c[k,i] = sin(1+k*d+i)/(H*d)` and
`L64(x) = sum(c * (F64(theta,x)-mu_x)/sx)`.

Construct c in NumPy float64. For input tensor j, construct `v_j=sin(r+1)/sqrt(sum(sin(r+1)^2))` in NumPy float64 over its row-major flattened coordinates; multiply columns by the original state scale for j=0 and input scale otherwise. Only that tensor changes; all other tangents are zero. This is the original numerical fixture construction. Preserve the actual original 32/64 direction arrays as a regression equality check; fresh cases use the same construction. Q means float32 cast then exact promotion to float64.

Use the entire fixed grid `h=(2^-7,2^-8,2^-9,2^-10)`, with both signs, every case, and all three input directions. No chosen/best h and no dropping a direction.

- **Nominal64 endpoints:** original x64 plus/minus h*v64, evaluated by F64 with original theta. These define the smooth local derivative consistency test.
- **Achieved32 endpoints:** begin at Q(x); calculate each endpoint in actual public float32 as `x32 +/- float32(h)*v32`, with `v32=Q(v64)`. Save the actual endpoint arrays, including unmodified tensors. Evaluate those exact promoted coordinates with F64 and original theta. This distinguishes endpoint rounding from prediction arithmetic without pretending an achieved step is the nominal one. A collapsed mandatory direction is unresolved evidence and cannot pass by omission.

The older decomposition's oracle rounded theta, c, mu and scales to Q32. Its saved arrays therefore remain diagnostic and cannot silently stand in for this full-precision oracle definition. Reuse its input/provenance and endpoint serialization helpers where contracts match; add the explicit full-theta path, without copying or replacing numerical model code.

## Prospective gates, with no tuned tolerance

1. **Unchanged forecast and AD parity.** Reuse the existing full33 numerical run as the original regression roster, and run the same worker/reducer over the copied packet with eight appended cases. Only its non-FD checks contribute to the new policy; every original and newly produced raw FD flag remains visible in a separate field and is not silently changed or incorporated through the runner's old overall-pass flag. Reuse the existing elementwise normalized comparisons on the fixed 41-case roster. Public32 compares to the original-theta oracle at Q(x), charging parameter/norm rounding to the public path. Keep all original forecast, JVP, reverse-gradient, transformation, duality, causality, shape, dtype and finiteness tolerances. In particular, public32 forecast uses atol1e-5/rtol1e-4, public32 AD uses2e-6/2e-3, public64 forecast uses1e-12/1e-10, and public64 AD uses1e-11/1e-9. No blanket exemption for real cases.
2. **Fixed Richardson consistency in64.** From nominal64 endpoint scalar values form all four central quotients `D_j=(L(x+h_j*v)-L(x-h_j*v))/(2*h_j)` and all three estimates `R_j=(4*D_(j+1)-D_j)/3`. Compute the independent oracle directional AD scalar A at original x64 with v64. Require both `abs(R_2-A) <= 2e-8+2e-5*abs(A)` and `abs(R_2-R_1) <= 2e-8+2e-5*abs(A)`. These use the existing x64 FD allowance. R_2 is prospectively fixed, never selected by results. Save every D/R and adjacent difference; R_0 remains visible. Near-zero A uses the same absolute allowance. Do not claim a rigorous Taylor remainder bound or assume an exact quartering rate; no adaptive step extension after failure.
3. **Finite32 response at achieved coordinates.** At every achieved plus/minus endpoint compare the public32 forecast to F64(original theta, achieved endpoint) in normalized coordinates, with the unchanged public32 forecast allowance. For a deterministic scalar summary, evaluate both saved predictions in NumPy64 using the same original c/mu/sx, then difference by2h. Propagate the existing endpoint allowances: `B(h)=sum(abs(c)*(b_plus+b_minus))/(2h)`, where each element `b_sign=1e-5+1e-4*abs(z64_sign)`. Require finite scalar response discrepancy<=B(h). This propagated bound is transparent and introduces no tuned derivative tolerance; the stronger mandatory checks are the endpoint checks themselves. Report achieved-step deviation, public32 scalar arithmetic and the original raw FD-versus-AD discrepancy separately. Do not charge a finite secant's truncation error to local AD or subtract it to rewrite the old flag.

All required grid cells must be available and finite; all listed gates pass on both regression and confirmation sets. Save old raw h32 failures and the new mathematical verdict in separate fields. Any new assessment permitting adoption replaces only the prospective numerical-assessment requirement: physical/capability/interface/integrity criteria still apply independently, and physical derivative fidelity remains unqualified.

## Execution and replay

Small extension of the existing no-fit numeric/FD workers and NumPy reducer only; no new harness architecture. Preserve the frozen preparation helper, which requires exactly24 actual cases. A new small input-only selector copies the authenticated original packet, appends eight schema-valid case entries and payloads, and writes a new execution manifest; it does not change that helper. The existing run/worker/reducer already iterate an arbitrary manifest roster. Preserve original case identities/order and bind the appended entries and copied payload hashes explicitly. Separate fixed-precision public/oracle processes, source/import/runtime attestations, exclusive first-attempt outputs and existing bounded worker watchdogs. Save predictions, scalar values, directions, nominal/achieved endpoints and all intermediate reductions, so fresh replay and an independent NumPy reduction need no fitting. Preserve partial failures and every failed flag. No automatic retry, source alteration, tolerance change or post-result case replacement.

The diagnostic anchor informing this proposal is run SHA256 `6074823469c7baa505eb35225467e366a51d97f26b4b7ca01520b18364f460aa`, diagnosis SHA256 `31c606ffad2b3b0b00cb1a568786f88ebba19c09ec8f283c0169d6aa1ea7de81`; this is explicitly development evidence. Original actual-input manifest SHA256 is `5a8f1ebd6007537bb9f9d751cd138c7e0400e9ff7180c5cd2771ef31df132797`. Original physical data seals: Crazyflow `f1acf0d0e305e48e08dc39fa5b9881577279e9947bc8b480bb6ddcdedc9093a2`; Cascade `7e2a18844412724bb38f861f24eee364de6d3c5b6d710bc718fd52e25dbe43ff`. The final freeze must include authenticated model/source anchors already used by the numerical packet and the unambiguous future selector source; no fresh-case identities are asserted before selection.

## Downstream public-adoption prerequisite, separate from the FD gate

The constant-materialization correction changes public inference source. Before any public promotion, freshly replay both corrected public precision arms over every saved physical query from both simulators, recompute the original physical metrics/decisions, and authenticate the unchanged historical reference predictions, models and truth/source inventories. Use the existing fixed-precision evaluation machinery. State the old-fit/new-inference source association explicitly. This requires no new collection or fitting and makes no new-cohort accuracy claim. The 24-case transition checks and33-case arithmetic suite establish only runtime and sampled numerical parity; old full-cohort outputs alone cannot qualify changed inference code. This prerequisite does not alter the proposed FD mathematics or its verdict.

## Freeze and implementation association

The corrected learner is inherited unchanged from `355fb75cbf3cc88582473bf18c641adfcb0ec79a`. The assessment implementation is `src/glassbox/experimental/public_mean_fd_assessment.py`; its only changes from the reviewed draft are the module description and an unused-variable rename required by repository lint. The external request binds this committed policy, the source map, original model/input/run anchors, and simulator seals before preparation. No learner or fitting change is part of this assessment. Synthetic selector and arithmetic integrity checks may execute after this freeze without touching physical targets.
